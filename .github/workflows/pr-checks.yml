# Two contributor-facing checks (separate jobs so failures are obvious):
#   Labels     — classifies the PR (new-card / US / enhancement / …). Never fails.
#   Form check — required title + beginner form fields. Fails when incomplete.
#
# TRIGGER: pull_request_target (NOT pull_request).
# ---------------------------------------------------------------------------
# For PRs opened from a FORK, GitHub downgrades the default GITHUB_TOKEN to
# read-only under `pull_request`, no matter what `permissions:` says. That
# silently breaks the whole point of this workflow: label writes no-op, the
# title auto-fix 403s, and the sticky form-check comment 403s (turning the
# Form check job red on a permission error rather than a real form problem).
# `pull_request_target` runs in the context of the BASE repo and therefore
# gets a WRITE token even for fork PRs, so triage keeps working for external
# contributors (this repo's target audience).
#
# SAFETY MODEL (pull_request_target is privileged — treat it carefully):
#   * NEVER check out the PR head ref. Every `actions/checkout@v4` below uses
#     no `ref:` / no fork `repository:` — that resolves to the BASE repo
#     default branch, i.e. trusted code only. We never run PR-authored code
#     (no build of fork sources, no fork-supplied npm scripts).
#   * The triage scripts (scripts/pr-triage.ts via scripts/run-pr-triage.ts,
#     scripts/build-base-cards.ts) are pure and come from the base checkout.
#   * Untrusted PR data (title/body/author) is passed ONLY through `env:`
#     values and read as `$PR_TITLE` etc. inside scripts — it is NEVER
#     interpolated as `${{ github.event.pull_request.title }}` into a `run:`
#     body (that would allow shell injection from a malicious title). The PR
#     number is likewise passed via `env: PR:` and used as `"$PR"`.
#   * Changed files (`gh pr diff`), the open-PR list (`gh pr list`) and base
#     card snapshots all work over the API / base checkout without touching
#     the head ref.

name: PR Checks

on:
  pull_request_target:
    types: [opened, edited, synchronize, reopened, ready_for_review]

concurrency:
  group: pr-checks-${{ github.event.pull_request.number }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: write
  issues: write

jobs:
  labels:
    name: Labels
    runs-on: ubuntu-latest
    steps:
      # No ref / no fork repository → base repo default branch (trusted code).
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Ensure classification labels exist
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          ensure() {
            local name="$1" color="$2" desc="$3"
            gh label create "$name" --color "$color" --description "$desc" 2>/dev/null || \
              gh label edit "$name" --color "$color" --description "$desc" 2>/dev/null || true
          }
          ensure "US" "0052CC" "Touches United States card data or images"
          ensure "CA" "D93F0B" "Touches Canada card data or images"
          ensure "CN" "FBCA04" "Touches China card data or images"
          ensure "new-card" "0E8A16" "Adds a new Card JSON file"
          ensure "enhancement" "a2eeef" "Non-card feature / CI / tooling change"
          ensure "documentation" "0075ca" "Docs-only change"

      - name: Collect changed files
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR: ${{ github.event.pull_request.number }}
        run: |
          gh pr diff "$PR" --name-only > /tmp/pr-files.txt || true

      - name: Classify PR
        env:
          PR_TITLE: ${{ github.event.pull_request.title }}
          PR_BODY: ${{ github.event.pull_request.body }}
          PR_TRIAGE_MODE: labels
        run: |
          export PR_FILES="$(cat /tmp/pr-files.txt)"
          node --experimental-strip-types scripts/run-pr-triage.ts > /tmp/triage.json
          cat /tmp/triage.json

      - name: Auto-fix PR title when we can infer it
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR: ${{ github.event.pull_request.number }}
        run: |
          node <<'NODE'
          const { execSync } = require("child_process");
          const t = require("/tmp/triage.json");
          if (t.titleOk || !t.suggestedTitle) process.exit(0);
          const pr = process.env.PR;
          console.log("Updating title to:", t.suggestedTitle);
          execSync(`gh pr edit ${pr} --title ${JSON.stringify(t.suggestedTitle)}`, {
            stdio: "inherit",
          });
          NODE
          # Re-classify after title fix
          export PR_TITLE="$(gh pr view "$PR" --json title -q .title)"
          export PR_BODY="$(gh pr view "$PR" --json body -q .body)"
          export PR_FILES="$(cat /tmp/pr-files.txt)"
          export PR_TRIAGE_MODE=labels
          node --experimental-strip-types scripts/run-pr-triage.ts > /tmp/triage.json

      - name: Apply classification labels only
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR: ${{ github.event.pull_request.number }}
        run: |
          ADD=$(node -e "const t=require('/tmp/triage.json'); console.log((t.classificationLabelsAdd||[]).join('\n'))")
          REM=$(node -e "const t=require('/tmp/triage.json'); console.log((t.classificationLabelsRemove||[]).join('\n'))")
          while IFS= read -r l; do
            [ -z "$l" ] && continue
            gh pr edit "$PR" --add-label "$l" || true
          done <<< "$ADD"
          while IFS= read -r l; do
            [ -z "$l" ] && continue
            gh pr edit "$PR" --remove-label "$l" || true
          done <<< "$REM"

  form-check:
    name: Form check
    needs: labels
    runs-on: ubuntu-latest
    steps:
      # No ref / no fork repository → base repo default branch (trusted code).
      # fetch-depth: 0 gives build-base-cards.ts full history so it can
      # `git show <base.sha>:<path>` the pre-PR version of each card file.
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Ensure completeness labels exist
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          ensure() {
            local name="$1" color="$2" desc="$3"
            gh label create "$name" --color "$color" --description "$desc" 2>/dev/null || \
              gh label edit "$name" --color "$color" --description "$desc" 2>/dev/null || true
          }
          ensure "needs-info" "D4C5F9" "Waiting on author for missing PR form fields"
          ensure "pr-form-incomplete" "E4E669" "PR body/title missing required beginner-form fields"
          ensure "missing-sources" "B60205" "Card PR is missing official source URLs"
          ensure "title-needs-fix" "F9D0C4" "PR title must be Conventional Commits: card(add|update): … or feat:/fix:/docs:/…"
          ensure "duplicate" "cfd3d7" "Likely duplicates another open card PR"

      - name: Collect changed files
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR: ${{ github.event.pull_request.number }}
        run: |
          gh pr diff "$PR" --name-only > /tmp/pr-files.txt || true

      - name: Collect open PRs + base card snapshots
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR: ${{ github.event.pull_request.number }}
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
        run: |
          # Other open PRs — used to detect duplicate card(add|update) work.
          # Best-effort: fall back to [] on transient gh/API failures (rate limits)
          # so the whole Form check never goes red on infrastructure errors.
          gh pr list --state open --limit 200 \
            --json number,title,url,author \
            --jq '[.[] | {number, title, url, author: .author.login}]' \
            > /tmp/open-prs.json || echo '[]' > /tmp/open-prs.json
          export PR_FILES="$(cat /tmp/pr-files.txt)"
          export BASE_SHA
          node --experimental-strip-types scripts/build-base-cards.ts > /tmp/base-cards.json
          echo "open PRs:" "$(wc -l < /tmp/open-prs.json)"
          echo "base cards:" "$(cat /tmp/base-cards.json)"

      - name: Check title and form fields
        id: check
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR: ${{ github.event.pull_request.number }}
          PR_TRIAGE_MODE: check
        run: |
          # Re-read title/body after Labels may have auto-fixed the title.
          export PR_TITLE="$(gh pr view "$PR" --json title -q .title)"
          export PR_BODY="$(gh pr view "$PR" --json body -q .body)"
          export PR_FILES="$(cat /tmp/pr-files.txt)"
          export PR_NUMBER="$PR"
          export OPEN_CARD_PRS_JSON="$(cat /tmp/open-prs.json)"
          export BASE_CARDS_JSON="$(cat /tmp/base-cards.json)"
          set +e
          node --experimental-strip-types scripts/run-pr-triage.ts > /tmp/triage.json
          code=$?
          set -e
          echo "exit=$code" >> "$GITHUB_OUTPUT"
          cat /tmp/triage.json

      - name: Apply completeness labels only
        if: always() && steps.check.outcome == 'success'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR: ${{ github.event.pull_request.number }}
        run: |
          ADD=$(node -e "const t=require('/tmp/triage.json'); console.log((t.completenessLabelsAdd||[]).join('\n'))")
          REM=$(node -e "const t=require('/tmp/triage.json'); console.log((t.completenessLabelsRemove||[]).join('\n'))")
          while IFS= read -r l; do
            [ -z "$l" ] && continue
            gh pr edit "$PR" --add-label "$l" || true
          done <<< "$ADD"
          while IFS= read -r l; do
            [ -z "$l" ] && continue
            gh pr edit "$PR" --remove-label "$l" || true
          done <<< "$REM"

      # Homebrew-style: always leave (or refresh) a PR comment before failing CI.
      - name: Comment on PR (Homebrew-style sticky helper)
        if: always() && steps.check.outcome == 'success'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR: ${{ github.event.pull_request.number }}
          PR_AUTHOR: ${{ github.event.pull_request.user.login }}
        run: |
          export PR_TITLE="$(gh pr view "$PR" --json title -q .title)"
          export PR_BODY="$(gh pr view "$PR" --json body -q .body)"
          export PR_FILES="$(cat /tmp/pr-files.txt)"
          export PR_NUMBER="$PR"
          export OPEN_CARD_PRS_JSON="$(cat /tmp/open-prs.json)"
          export BASE_CARDS_JSON="$(cat /tmp/base-cards.json)"
          export PR_TRIAGE_MODE=check
          export PR_AUTHOR
          # Mask exit 1 from incomplete forms — we still need the JSON to write the comment.
          set +e
          node --experimental-strip-types scripts/run-pr-triage.ts > /tmp/triage-comment.json
          set -e
          node -e "const t=require('/tmp/triage-comment.json'); require('fs').writeFileSync('/tmp/form-comment.md', t.commentMarkdown)"
          echo "----- comment preview -----"
          cat /tmp/form-comment.md
          echo "---------------------------"

          upsert() {
            local marker="$1"
            local id
            id=$(gh api "repos/${GITHUB_REPOSITORY}/issues/${PR}/comments" --paginate \
              --jq ".[] | select(.body | contains(\"${marker}\")) | .id" | head -n1)
            if [ -n "$id" ]; then
              gh api -X PATCH "repos/${GITHUB_REPOSITORY}/issues/comments/${id}" \
                -F body=@/tmp/form-comment.md
              return 0
            fi
            return 1
          }

          if upsert "opencard-form-check"; then
            echo "Updated sticky form-check comment."
          elif upsert "opencard-triage"; then
            echo "Updated legacy triage comment → form-check body."
          else
            gh pr comment "$PR" --body-file /tmp/form-comment.md
            echo "Posted new form-check comment."
          fi

      - name: Fail when title or required form fields are incomplete
        if: always() && steps.check.outcome == 'success'
        run: |
          node -e "const t=require('/tmp/triage.json'); const errs=(t.issues||[]).filter(i=>i.severity==='error'); process.exit((!t.titleOk || errs.length || (t.missing||[]).length) ? 1 : 0)"
